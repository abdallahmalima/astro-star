<script>
const listItems = document.querySelectorAll('.hover-active');
let currentIndex = 0;
const clickNextLink = () => {

  listItems[currentIndex].click();
  currentIndex++;

  if (currentIndex >= listItems.length) {
    currentIndex = 0;
  }
};

const interval = setInterval(clickNextLink, 5000);
</script>